package com.secondhand.model;

import org.anddev.andengine.extension.physics.box2d.PhysicsWorld;

import junit.framework.TestCase;

import com.secondhand.model.entity.GameWorld;
import com.secondhand.model.physics.IPhysicsWorld;
import com.secondhand.model.physics.Vector2;
import com.secondhand.view.physics.MyPhysicsWorld;

public class GameWorldTest extends TestCase {

	public void testConstructor() {
		/*
		 * int lives = 3; int levelNumber = 1; int score = 100;
		 * 
		 * IPhysicsWorld physics = new MyPhysicsWorld(new PhysicsWorld(new
		 * com.badlogic.gdx.math.Vector2(), true)); GameWorld gWorld = new
		 * GameWorld(physics,levelNumber, lives, score);
		 * 
		 * assertEquals(lives, gWorld.getLevelNumber()); assertEquals(score,
		 * gWorld.getPlayer().getScore()); assertEquals(lives,
		 * gWorld.getPlayer().getLives()); assertEquals(physics,
		 * gWorld.getPhysics());
		 */
	}
}
